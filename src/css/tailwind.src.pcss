@tailwind base;

h1 {
  @apply text-2xl;
  @apply font-semibold;
}
h2 {
  @apply text-xl;
  @apply font-semibold;
}
h3 {
  @apply text-lg;
  @apply font-semibold;
}
h4 {
  @apply text-base;
  @apply font-semibold;
}
a {
  @apply text-blue-600 
}
@tailwind components;
@tailwind utilities;
