import EsittelijatContainer from 'routes/Esittelijat/containers/EsittelijatContainer'

const routes = [
  {
    path: '/esittelijat',
    exact: true,
    component: EsittelijatContainer
  }
]

export default routes
